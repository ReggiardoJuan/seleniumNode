name: CI

on:
  workflow_dispatch:

  pull_request:
    types: [opened, synchronize, labeled, unlabeled, reopened, edited, closed]

env:
  NODE_OPTIONS: --max-old-space-size=16384

jobs:
  check_ticket:
    name: Check task number is present in PR description
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
        - name: Get context
          run: |
              echo "${{toJson(github)}}"

  # update_board_task_status:
  #   name: Update board task status on PR merge
  #   runs-on: ubuntu-latest      merged: false,
  #   steps:
  #       - name: Get context
  #         run: |
  #             echo "${{toJson(github)}}"
       # - id: get_pr
       #   name: Get task id from associated pull request
       #   run: |
       #      pullRequest="$(gh api /repos/$REPOSITORY/commits/$EVENT_SHA/pulls)"
       #      echo "PULL_REQUEST=$pullRequest" >> $GITHUB_ENV
       #   env:
       #      REPOSITORY: ${{ github.repository }}
       #      EVENT_SHA: ${{ github.sha }}
       #      GH_TOKEN: ${{ secrets.TOKEN }}
       # - id: get_task
       #   name: Get task-id from body comment
       #   uses: actions/github-script@v6
       #   with:
       #    result-encoding: string
       #    script: |
       #      const [{ body }] = ${{ env.PULL_REQUEST }};
       #      return body.match(/(?<=AB#)[0-9]+/)[0];
